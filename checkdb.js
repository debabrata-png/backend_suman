const mongoose = require('mongoose');
const Exammarks1ds = require('./Models/exammarks1ds');

mongoose.connect('mongodb+srv://user:pass@cluster.mongodb.net/dbname') // I don't know the connection string
